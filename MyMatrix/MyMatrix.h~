#pragma once
#ifndef MYMATRIX_H
#define MYMATRIX_H
#include <vector>
class MyMatrix
{
public:
	MyMatrix(void);
	~MyMatrix(void);
private:
	int rows;
	int cols;
	int nums;
	vector<double> data;
	vector<unsigned int> 
};

#endif 